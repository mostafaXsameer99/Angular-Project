import { MyPercentPipePipe } from './my-percent-pipe.pipe';

describe('MyPercentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyPercentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
